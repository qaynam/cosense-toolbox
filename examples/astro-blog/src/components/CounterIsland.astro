---
/**
 * Svelte のコンポーネントをブラウザで動かすには client: ディレクティブが要る。
 * ディレクティブは .astro の中でしか付けられないので、ここで包んでから cosense.ts で渡す。
 */
import Counter from "./Counter.svelte"

const { start, step } = Astro.props
---

<Counter client:load start={start} step={step} />
