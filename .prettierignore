# .gitignore に書いたもの (dist / .astro / node_modules / .turbo / .wrangler など) は、prettier が自動で除く。
bun.lock
**/public/**
# 生成したファイル。作り直すたびに差分が出ないよう、整形しない。
**/*.generated.ts
# 入れ子の .gitignore は prettier に読まれない。ビルドの出力をここでも除く。
packages/textmate/grammars/
apps/zed-cosense-x/target/
