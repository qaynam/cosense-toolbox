# @cosense-toolbox/* は公開レジストリを見る。
# 既定のレジストリ (~/.npmrc の私設レジストリ) はそのままにして、このスコープだけを切り替える。
# publish は package.json の publishConfig.registry が効くが、
# login / whoami / view は publishConfig を読まないため、ここで指定しておく。
@cosense-toolbox:registry=https://registry.npmjs.org/
